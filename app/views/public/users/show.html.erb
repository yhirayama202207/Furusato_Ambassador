<h5>マイページ</h5>

</br>

&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;<label>登録情報</label>&emsp;<%= link_to "編集する", edit_user_path, class: "btn btn-success" %>
</br>
</br>

<div class= "col-1 mt-2">
<%#= form_with model: @item, url: cart_items_path, method: :post do |f| %>
<% if @user.image.attached? %>
  <%= image_tag @user.image, size: "200x200" %>
<% else %>
  <%= image_tag 'no_image.jpg', size: "200x200" %>
<% end %>
</div>

 <div class= "col-md-8 offset-md-1">
  <table class="table table-hover table-bordered table-inverse">
    <tbody>
      <tr>
        <th class="table-secondary">ユーザー名</th>
        <td>
          <%= @user.user_name %>
        </td>
      </tr>
      <tr>
        <th class="table-secondary">メールアドレス</th>
        <td>
          <%= @user.email %>
        </td>
      </tr>
      <tr>
        <th class="table-secondary">居住地</th>
        <td>
          <%= @user.home %>
        </td>
      </tr>
      <tr>
        <th class="table-secondary">出身地</th>
        <td>
          <%= @user.hometown %>
        </td>
      </tr>
    </tbody>
  </table>
</br>

記事履歴&emsp;<%#= link_to "一覧を見る", articles_path(@user.id), class: "btn btn-primary" %>